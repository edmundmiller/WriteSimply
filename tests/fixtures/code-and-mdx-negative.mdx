# Code and MDX exclusions

The example uses direct prose around its code.

`utilize prior to in order to it is worth noting that make use of added bonus`

```python
message = "It is important to note that this code can make use of a completely unanimous result in order to describe implementation details without producing prose alerts."
```

<Result
  label="It is worth noting that this component explains the basic fundamentals"
  query="take utilization into consideration in order to sort results"
/>

<code>Needless to say, the unexpected surprise made use of data prior to review.</code>
