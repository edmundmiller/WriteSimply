# Code and MDX exclusions

The example uses direct prose around its code.

`utilize prior to in order to it is worth noting that`

```python
message = "It is important to note that this code can utilize a large number of words in order to describe implementation details without producing prose alerts."
```

<Result
  label="It is worth noting that this component utilizes data prior to review"
  query="select utilization from metrics in order to sort results"
/>

<code>Needless to say, utilize data prior to review.</code>
